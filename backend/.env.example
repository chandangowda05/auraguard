# Smart Wristband Server Config
PORT=5000

# MongoDB URI connection string
# Leave empty or keep local URI. If local connection fails, server automatically runs in in-memory simulation mode!
MONGO_URI=mongodb://localhost:27017/smart_wristband

# Secret key used for signing dashboard login JWTs
JWT_SECRET=smart_wristband_secret_key_123456
